{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"TeleGlance","text":"<p>Async Python client for public Telegram channels. No API keys. No MTProto. No account.</p> <p>TeleGlance reads Telegram's public t.me web previews to give you channel metadata, message history, search, live monitoring, and media downloads \u2014 without touching the Telegram API.</p> <pre><code>import asyncio\nfrom teleglance import TeleGlanceClient\n\n\nasync def main():\n    async with TeleGlanceClient() as client:\n        channel = await client.get_channel(\"nahomssandbox\")\n        print(channel.title, channel.counts.subscribers)\n\n        async for message in client.iter_messages(\"nahomssandbox\", limit=10):\n            print(message.id, message.date, message.text[:80])\n\n\nasyncio.run(main())\n</code></pre> <ul> <li> <p> Getting Started</p> <p>Install TeleGlance and run your first query in under five minutes.</p> <p> Install</p> </li> <li> <p> API Reference</p> <p>Full documentation of every method, parameter, model, and exception.</p> <p> Reference</p> </li> <li> <p> Checkpoints</p> <p>Resumable scraping with <code>oldest_id</code> and <code>newest_id</code> watermarks.</p> <p> Checkpoints guide</p> </li> <li> <p> Media Downloads</p> <p>Download photos, videos, and voice notes. Understand what cannot be downloaded and why.</p> <p> Media guide</p> </li> <li> <p> Advanced Customization</p> <p>Override CSS selectors, add transport hooks, extend the parser registry, configure proxies.</p> <p> Customization guide</p> </li> <li> <p> GitHub</p> <p>Source code, issues, and contributions.</p> <p> NWGKGIT/TeleGlance</p> </li> </ul>"},{"location":"#why-teleglance","title":"Why TeleGlance?","text":"<p>TeleGlance is not a replacement for Telethon. It is for applications that only need public channel data and do not want API credentials or an MTProto session.</p> Capability TeleGlance Telethon Telegram API credentials No Yes MTProto No Yes Public channels Yes Yes Private channels No Yes Sending messages No Yes Account/session required No Generally"},{"location":"#what-it-cannot-do","title":"What it cannot do","text":"<ul> <li>Access private channels or groups</li> <li>Send messages or interact with chats</li> <li>Download document attachments (no direct URL in web previews)</li> <li>Guarantee complete channel history (Telegram caps preview depth)</li> <li>Replace a full Telegram API client</li> </ul>"},{"location":"advanced_customization/","title":"Advanced Customization","text":"<p>TeleGlance is designed to handle markup changes and allow low-level networking adjustments without code changes.</p>"},{"location":"advanced_customization/#transport-hooks","title":"Transport Hooks","text":"<p>Observability hooks use HTTPX native event hooks. Pass functions into <code>request_hooks</code> or <code>response_hooks</code> to intercept network events:</p> <pre><code>import httpx\nfrom teleglance import TeleGlanceClient\n\n\nasync def on_request(request: httpx.Request):\n    print(f\"Request: {request.method} {request.url}\")\n\n\nasync def on_response(response: httpx.Response):\n    print(f\"Response: {response.status_code}\")\n\n\nasync def main():\n    async with TeleGlanceClient(request_hooks=[on_request], response_hooks=[on_response]) as client:\n        await client.get_channel(\"nahomssandbox\")\n</code></pre>"},{"location":"advanced_customization/#selectors-override","title":"Selectors Override","text":"<p>All HTML classes and attributes are stored in a centralized <code>Selectors</code> configuration class. If Telegram alters class names, pass overrides directly:</p> <pre><code>from teleglance import DEFAULT_SELECTORS, TeleGlanceClient\n\n# Change views count selector without changing library files\ncustom_selectors = DEFAULT_SELECTORS.replace(views=\".tgme_widget_message_views_updated\")\n\nclient = TeleGlanceClient(selectors=custom_selectors)\n</code></pre> <p>You can also load custom selectors from a JSON file:</p> <pre><code>import json\nfrom teleglance import Selectors, TeleGlanceClient\n\nwith open(\"selectors.json\") as f:\n    selectors = Selectors.from_dict(json.load(f))\n\nclient = TeleGlanceClient(selectors=selectors)\n</code></pre>"},{"location":"advanced_customization/#custom-parser-registry","title":"Custom Parser Registry","text":"<p>If Telegram rolls out a new widget block, you can write and register custom parsing logic.</p> <p>A parsing function receives the parsed message node (a <code>selectolax</code> parser node) and returns a list of media structures or fallbacks:</p> <pre><code>from teleglance import TeleGlanceClient, Unsupported, default_registry\n\n# 1. Initialize registry with default parsers\nregistry = default_registry()\n\n\n# 2. Define custom parsing function\ndef parse_gift_widget(message_node):\n    block = message_node.css_first(\".tgme_widget_message_gift\")\n    if block:\n        # Return Unsupported model carrying the raw HTML\n        return [Unsupported(raw_html=block.html)]\n    return []\n\n\n# 3. Register custom function\nregistry.register(\"gift\", parse_gift_widget)\n\n# 4. Instantiate client\nclient = TeleGlanceClient(registry=registry)\n</code></pre>"},{"location":"advanced_customization/#proxies","title":"Proxies","text":"<p>Specify proxies using standard format strings:</p> <pre><code># HTTP/HTTPS Proxy\nclient = TeleGlanceClient(proxy=\"http://127.0.0.1:8080\")\n\n# SOCKS5 Proxy (Requires [socks] installation extra)\nclient = TeleGlanceClient(proxy=\"socks5://127.0.0.1:9050\")\n</code></pre>"},{"location":"api_reference/","title":"API Reference","text":"<p>Detailed specifications of the classes, client methods, data models, and custom exceptions in TeleGlance.</p>"},{"location":"api_reference/#teleglanceclient","title":"TeleGlanceClient","text":"<p>The main client interface for communicating with Telegram web previews.</p>"},{"location":"api_reference/#constructor","title":"Constructor","text":"<pre><code>class TeleGlanceClient:\n    def __init__(\n        self,\n        *,\n        base_url: str = \"https://t.me\",\n        rate_limit: float = 1.0,\n        retries: int = 3,\n        timeout: float = 15.0,\n        proxy: str | None = None,\n        headers: dict[str, str] | None = None,\n        request_hooks: list[RequestHook] | None = None,\n        response_hooks: list[ResponseHook] | None = None,\n        registry: ParserRegistry | None = None,\n        selectors: Selectors | None = None,\n        transport: TransportProtocol | None = None,\n        strict_parsing: bool = False,\n    )\n</code></pre> <ul> <li>Parameters:</li> <li><code>base_url</code>: Target URL for Telegram previews. Defaults to <code>https://t.me</code>.</li> <li><code>rate_limit</code>: Maximum requests per second. Defaults to <code>1.0</code>. Set to <code>0</code> to disable request throttling.</li> <li><code>retries</code>: Number of retry attempts on HTTP failure or 429 status codes. Defaults to <code>3</code>.</li> <li><code>timeout</code>: Request timeout in seconds. Defaults to <code>15.0</code>.</li> <li><code>proxy</code>: Proxy server URL (HTTP, HTTPS, SOCKS4, SOCKS5).</li> <li><code>headers</code>: Custom headers to merge into request headers.</li> <li><code>request_hooks</code>: Callbacks executed before sending requests.</li> <li><code>response_hooks</code>: Callbacks executed after receiving responses.</li> <li><code>registry</code>: ParserRegistry containing custom parsing functions.</li> <li><code>selectors</code>: Custom Selectors mapping for extraction.</li> <li><code>strict_parsing</code>: When enabled, recognized pages containing layout containers but no posts raise a ParseError instead of returning empty lists.</li> </ul>"},{"location":"api_reference/#async-methods","title":"Async Methods","text":""},{"location":"api_reference/#get_channel","title":"<code>get_channel</code>","text":"<p>Retrieve metadata details for a public Telegram channel. <pre><code>async def get_channel(self, channel: str) -&gt; Channel\n</code></pre> * Parameters:   * <code>channel</code>: Target channel identifier (e.g. name, @name, link). * Raises:   * <code>ChannelNotFound</code>: If the channel does not exist on Telegram.   * <code>ChannelPrivate</code>: If the channel is private.</p>"},{"location":"api_reference/#get_message","title":"<code>get_message</code>","text":"<p>Fetch a single message by ID. <pre><code>async def get_message(self, channel: str, msg_id: int) -&gt; Message\n</code></pre> * Parameters:   * <code>channel</code>: Target channel identifier.   * <code>msg_id</code>: Message ID to search for. Must be a positive integer. * Raises:   * <code>MessageNotFound</code>: If the message does not exist or has been deleted.</p>"},{"location":"api_reference/#iter_messages","title":"<code>iter_messages</code>","text":"<p>Iterate channel posts backwards (newest to oldest). <pre><code>async def iter_messages(\n    self,\n    channel: str,\n    *,\n    limit: int | None = None,\n    before: int | None = None,\n    query: str | None = None,\n) -&gt; AsyncIterator[Message]\n</code></pre> * Parameters:   * <code>channel</code>: Target channel identifier.   * <code>limit</code>: Maximum number of messages to yield.   * <code>before</code>: Message ID cursor. Yields posts strictly older than this ID.   * <code>query</code>: Filter messages by keyword search.</p>"},{"location":"api_reference/#iter_new_messages","title":"<code>iter_new_messages</code>","text":"<p>Iterate channel posts forwards (oldest to newest). <pre><code>async def iter_new_messages(\n    self,\n    channel: str,\n    *,\n    after: int,\n    limit: int | None = None,\n) -&gt; AsyncIterator[Message]\n</code></pre> * Parameters:   * <code>channel</code>: Target channel identifier.   * <code>after</code>: Message ID cursor. Yields posts strictly newer than this ID.   * <code>limit</code>: Maximum number of messages to yield.</p>"},{"location":"api_reference/#search","title":"<code>search</code>","text":"<p>Search channel messages. An alias for <code>iter_messages</code> with a query filter. <pre><code>def search(\n    self,\n    channel: str,\n    query: str,\n    *,\n    limit: int | None = None,\n) -&gt; AsyncIterator[Message]\n</code></pre></p>"},{"location":"api_reference/#watch","title":"<code>watch</code>","text":"<p>Start a live poller that yields new posts. <pre><code>def watch(\n    self,\n    channel: str,\n    *,\n    interval: float = 30.0,\n    since_id: int | None = None,\n) -&gt; AsyncIterator[Message]\n</code></pre> * Parameters:   * <code>channel</code>: Target channel identifier.   * <code>interval</code>: Polling interval in seconds. Defaults to <code>30.0</code>.   * <code>since_id</code>: Filter posts newer than this ID. Defaults to the newest post ID found on startup.</p>"},{"location":"api_reference/#download_media","title":"<code>download_media</code>","text":"<p>Download media attachments to disk. <pre><code>async def download_media(\n    self,\n    media: Media | str,\n    dest: str | Path | None = None,\n    *,\n    filename: str | None = None,\n    overwrite: bool = False,\n    max_bytes: int | None = None,\n) -&gt; Path\n</code></pre> * Parameters:   * <code>media</code>: Target Media object or direct download URL string.   * <code>dest</code>: Directory path or full file destination path.   * <code>filename</code>: Optional filename override.   * <code>overwrite</code>: Set to True to replace existing files.   * <code>max_bytes</code>: Exceeding this size raises a DownloadError.</p>"},{"location":"api_reference/#download_bytes","title":"<code>download_bytes</code>","text":"<p>Download media attachments directly into memory. <pre><code>async def download_bytes(\n    self,\n    media: Media | str,\n    *,\n    max_bytes: int | None = None,\n) -&gt; bytes\n</code></pre></p>"},{"location":"api_reference/#data-models","title":"Data Models","text":"<p>Pydantic V2 models returned by TeleGlance.</p>"},{"location":"api_reference/#channel","title":"Channel","text":"<p>Represents public Telegram channel details. * <code>username</code> (<code>str</code>): Target handle of the channel. * <code>url</code> (<code>str</code>): Direct t.me/s/ URL. * <code>title</code> (<code>str</code>): Title of the channel. * <code>description</code> (<code>str | None</code>): Text description. * <code>description_html</code> (<code>str | None</code>): HTML description. * <code>avatar_url</code> (<code>str | None</code>): Image URL. * <code>counts</code> (<code>ChannelCounts</code>): Approximate subscriber and media attachment counters.</p>"},{"location":"api_reference/#message","title":"Message","text":"<p>Represents a single Telegram post. * <code>id</code> (<code>int</code>): Unique message ID. * <code>channel</code> (<code>str</code>): Channel handle. * <code>url</code> (<code>str</code>): Direct message URL. * <code>date</code> (<code>datetime | None</code>): Publish timestamp. * <code>views</code> (<code>int | None</code>): Estimated view count. * <code>views_str</code> (<code>str | None</code>): Raw views text (e.g. \"1.2K\"). * <code>author</code> (<code>str | None</code>): Signature of post author. * <code>text</code> (<code>str</code>): Text content. * <code>html</code> (<code>str | None</code>): Inner HTML of post content. * <code>markdown</code> (<code>str | None</code>): Lossy markdown formatting. * <code>entities</code> (<code>list[Entity]</code>): Text styles, hyperlinks, and mentions. * <code>media</code> (<code>list[Media]</code>): Media attachments. * <code>reactions</code> (<code>list[Reaction]</code>): Active emoji reactions. * <code>raw_html</code> (<code>str</code>): Complete HTML node string.</p>"},{"location":"api_reference/#custom-exceptions","title":"Custom Exceptions","text":"<ul> <li><code>TeleGlanceError</code>: Base package exception.</li> <li><code>InvalidChannel</code>: Raised when channel identifier formats are invalid.</li> <li><code>ChannelNotFound</code>: Raised when a public channel is not found.</li> <li><code>ChannelPrivate</code>: Raised when a channel is private or previews are blocked.</li> <li><code>MessageNotFound</code>: Raised when a specific message ID does not exist.</li> <li><code>ParseError</code>: Raised when the parser fails to understand layout structures.</li> <li><code>RateLimited</code>: Raised when requests exceed Telegram rate limits.</li> <li><code>DownloadError</code>: Raised when media downloads fail or exceed limits.</li> <li><code>CheckpointError</code>: Raised when reading or writing checkpoints fails.</li> </ul>"},{"location":"checkpoints_resumability/","title":"Checkpoints and Resumability","text":"<p>TeleGlance includes checkpointing utilities to track scraping progress. If your scraping process halts due to crashes, network interruptions, or rate limits, you can resume precisely where you left off.</p>"},{"location":"checkpoints_resumability/#watermarks","title":"Watermarks","text":"<p>Checkpointing works by keeping track of two bounds (watermarks) for a given channel:</p> <ol> <li><code>oldest_id</code>: The lowest message ID you have processed. Used for paginating backward into the past.</li> <li><code>newest_id</code>: The highest message ID you have processed. Used for forward collection and monitoring updates.</li> </ol>"},{"location":"checkpoints_resumability/#jsoncheckpointstore","title":"JsonCheckpointStore","text":"<p><code>JsonCheckpointStore</code> is a single-writer checkpoint tracker included in the package. It writes updates atomically to a local JSON file.</p>"},{"location":"checkpoints_resumability/#backwards-collection-history","title":"Backwards Collection (History)","text":"<p>To scrape history backwards without reprocessing messages, pass the <code>oldest_id</code> to <code>before</code>:</p> <pre><code>import asyncio\nfrom teleglance import JsonCheckpointStore, MessageCheckpoint, TeleGlanceClient\n\n\nasync def main():\n    # Initialize store pointing to a local file\n    store = JsonCheckpointStore(\"scraper_state.json\")\n\n    # Load existing state, or fall back to a empty checkpoint\n    state = await store.load(\"history:nahomssandbox\") or MessageCheckpoint(channel=\"nahomssandbox\")\n\n    async with TeleGlanceClient() as client:\n        # Pass state.oldest_id to prevent duplicate scraping\n        async for message in client.iter_messages(\n            \"nahomssandbox\", before=state.oldest_id, limit=50\n        ):\n            # Process the message\n            print(f\"Scraped history message: {message.id}\")\n\n            # Record changes to watermarks\n            state = state.record(message)\n\n            # Save checkpoint state atomically\n            await store.save(\"history:nahomssandbox\", state)\n\n\nif __name__ == \"__main__\":\n    asyncio.run(main())\n</code></pre>"},{"location":"checkpoints_resumability/#forwards-collection-new-posts","title":"Forwards Collection (New Posts)","text":"<p>To fetch posts created since your last check, use <code>iter_new_messages</code> with <code>after</code>:</p> <pre><code>async def check_updates():\n    store = JsonCheckpointStore(\"scraper_state.json\")\n    state = await store.load(\"history:nahomssandbox\")\n\n    if not state or state.newest_id is None:\n        print(\"No previous checkpoint state found.\")\n        return\n\n    async with TeleGlanceClient() as client:\n        async for message in client.iter_new_messages(\"nahomssandbox\", after=state.newest_id):\n            print(f\"Scraped new update: {message.id}\")\n            state = state.record(message)\n            await store.save(\"history:nahomssandbox\", state)\n</code></pre>"},{"location":"checkpoints_resumability/#custom-checkpoint-stores","title":"Custom Checkpoint Stores","text":"<p>The checkpoint store implements a basic <code>CheckpointStore</code> protocol. You can substitute the default JSON store with custom storage solutions (such as Redis, SQLite, or PostgreSQL) by implementing <code>load</code> and <code>save</code> methods:</p> <pre><code>from typing import Protocol\nfrom teleglance import MessageCheckpoint\n\n\nclass CheckpointStore(Protocol):\n    async def load(self, key: str) -&gt; MessageCheckpoint | None: ...\n\n    async def save(self, key: str, checkpoint: MessageCheckpoint) -&gt; None: ...\n</code></pre>"},{"location":"getting_started/","title":"Getting Started","text":"<p>Learn how to install, configure, and verify your TeleGlance installation.</p>"},{"location":"getting_started/#installation","title":"Installation","text":"<p>TeleGlance requires Python 3.10 or newer.</p>"},{"location":"getting_started/#using-pip","title":"Using pip","text":"<p>To install the core library: <pre><code>pip install teleglance\n</code></pre></p>"},{"location":"getting_started/#using-uv","title":"Using uv","text":"<p>For modern Python projects using <code>uv</code>: <pre><code>uv add teleglance\n</code></pre></p>"},{"location":"getting_started/#installation-extras","title":"Installation Extras","text":"<p>TeleGlance packages optional dependencies as extras to keep the core installation lightweight.</p>"},{"location":"getting_started/#cli-utility-cli","title":"CLI Utility (<code>[cli]</code>)","text":"<p>Installs command line capabilities along with packages like <code>click</code> and <code>rich</code>. <pre><code>pip install \"teleglance[cli]\"\n# or\nuv add \"teleglance[cli]\"\n</code></pre></p>"},{"location":"getting_started/#socks-proxy-support-socks","title":"SOCKS Proxy Support (<code>[socks]</code>)","text":"<p>Installs SOCKS proxy libraries (like <code>socksio</code>) to enable SOCKS4 or SOCKS5 proxy routing. <pre><code>pip install \"teleglance[socks]\"\n# or\nuv add \"teleglance[socks]\"\n</code></pre></p>"},{"location":"getting_started/#verify-your-installation","title":"Verify Your Installation","text":"<p>Create a new Python file named <code>test_teleglance.py</code> with the following content to verify that the client connects and reads data correctly from the example channel, <code>nahomssandbox</code>:</p> <pre><code>import asyncio\nfrom teleglance import TeleGlanceClient\n\n\nasync def main():\n    async with TeleGlanceClient() as client:\n        # Fetch target channel details\n        channel = await client.get_channel(\"nahomssandbox\")\n        print(f\"Connection Successful!\")\n        print(f\"Channel Title: {channel.title}\")\n        print(f\"Subscribers: {channel.counts.subscribers}\")\n\n        # Fetch and print the latest message text\n        async for message in client.iter_messages(\"nahomssandbox\", limit=1):\n            print(f\"Latest Post text: {message.text[:120]}\")\n\n\nif __name__ == \"__main__\":\n    asyncio.run(main())\n</code></pre> <p>Run the script: <pre><code>python test_teleglance.py\n# or\nuv run test_teleglance.py\n</code></pre> If you see the channel title and subscribers printed to the terminal, your environment is correctly configured.</p>"},{"location":"media_downloads/","title":"Media Downloads","text":"<p>TeleGlance allows you to download files and assets attached to Telegram posts.</p>"},{"location":"media_downloads/#download-methods","title":"Download Methods","text":"<p>You can download media using two client methods:</p> <ol> <li><code>download_media</code>: Save media directly to your filesystem.</li> <li><code>download_bytes</code>: Retrieve the media content directly in-memory as a <code>bytes</code> object.</li> </ol>"},{"location":"media_downloads/#code-example","title":"Code Example","text":"<pre><code>import asyncio\nfrom teleglance import TeleGlanceClient\n\n\nasync def main():\n    async with TeleGlanceClient() as client:\n        async for message in client.iter_messages(\"nahomssandbox\", limit=10):\n            # Check if post contains attachments\n            for media in message.media:\n                if media.type == \"photo\":\n                    # Download photo to local ./downloads folder\n                    saved_path = await client.download_media(media, dest=\"./downloads\")\n                    print(f\"Photo saved: {saved_path}\")\n\n                elif media.type == \"sticker\":\n                    # Retrieve sticker bytes directly\n                    image_bytes = await client.download_bytes(media)\n                    print(f\"Sticker downloaded. Size: {len(image_bytes)} bytes\")\n\n\nif __name__ == \"__main__\":\n    asyncio.run(main())\n</code></pre>"},{"location":"media_downloads/#filename-handling-file-length-limits","title":"Filename Handling &amp; File Length Limits","text":"<p>Telegram CDN URLs contain long hashes as file identifiers. These can produce filenames that exceed typical OS limits (usually 255 characters). </p> <ul> <li>Automatic Truncation: TeleGlance truncates generated filenames to a maximum of 200 characters to ensure that temporary file operations (which add prefixes or suffixes) do not cause filesystem errors.</li> <li>Custom Naming: If you want to use a specific name, pass a <code>filename</code> argument to the <code>download_media</code> function:   <pre><code>await client.download_media(photo, dest=\"./downloads\", filename=\"logo.jpg\")\n</code></pre></li> </ul>"},{"location":"media_downloads/#important-media-limitations","title":"Important Media Limitations","text":"<p>When working with public Telegram web previews, keep the following limits in mind:</p> <ol> <li>No Document Downloads: Telegram web previews do not include direct URLs for document attachments. You can retrieve document metadata (e.g. filename, title, extra sizes), but calling download methods on a document will raise a <code>DownloadError</code>.</li> <li>Missing Video Links: Large video files may only expose a <code>thumb_url</code> instead of a full video download <code>url</code> in the web preview HTML structure.</li> <li>Throttled Speed: Media downloads use the same HTTP Transport layer. This means they are subject to client rate-limiting and connection policies. Use SOCKS/HTTP proxies if you need to run high-volume download pipelines.</li> </ol>"},{"location":"guides/","title":"Guides","text":"<p>Select a guide from the navigation or the cards below.</p> <ul> <li> <p> Checkpoints &amp; Resumability</p> <p>Resume interrupted scraping jobs without reprocessing messages.</p> <p> Read guide</p> </li> <li> <p> Media Downloads</p> <p>Download photos, videos, and voice notes. Understand document limitations.</p> <p> Read guide</p> </li> <li> <p> Advanced Customization</p> <p>Transport hooks, selector overrides, parser registry, and proxy configuration.</p> <p> Read guide</p> </li> </ul>"}]}