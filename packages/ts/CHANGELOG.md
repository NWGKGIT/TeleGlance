# Changelog

## 0.1.1 - 2026-08-14

- Add public `dumpJson`, `captureJson`, and `appendJson` capture helpers.
- Add CLI `--output` and safe `--overwrite` options for channel, message, search, and watch captures.

## 0.1.0 - 2026-08-14

Initial TypeScript and Node.js release, including public-channel parsing, pagination, media downloads, checkpoints, live polling, and the command-line interface.
